node_modules/
dist/
playwright-report/
test-results/
debug.log
.DS_Store

# Playwright MCP tool's own scratch output (page snapshots, console logs,
# ad hoc verification screenshots) -- not project artifacts.
.playwright-mcp/

# Worktree directory
.worktrees/

# Subagent-driven-development scratch workspace
.superpowers/

# Human-review recordings (`npm run review:clips`): review material, not
# artifacts -- regenerate them from the seed instead of committing ~100 MB of
# video. The filled-in review document beside them IS committed.
docs/reviews/clips/